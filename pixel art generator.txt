pixel art generator

PROGRAMMING LANGUAGE:

PURPOSE: take an image, make it a pixelated image (like minecraft paintings)

FEATURES:
changeable pixel width/height
self-imported images
need a self-developed algorithm to create such pixels
downloadable/printable buttons

KEY ALGORITHM:
https://en.wikipedia.org/wiki/Pixel-art_scaling_algorithms (scaling UPWARDS, not down)
would it be possible to include multiple

for every pixel/cell, display the most frequent color? (but how would that work if every color is different)
